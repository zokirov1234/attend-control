spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/attend_control
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081
